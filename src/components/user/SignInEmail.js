import React, { Fragment } from 'react'

const SignInEmail = () => {
    return (
        <Fragment>
            <input type="text" />
            <input type="password" />
            <button>로그인</button>
        </Fragment>
    );
};

export default SignInEmail;